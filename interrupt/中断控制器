高级可编程中断控制器（APIC）
现代系统通常使用高级可编程中断控制器（APIC），如Intel的I/O APIC和本地APIC。APIC的工作流程如下：

中断请求：硬件设备生成中断信号，并发送到I/O APIC的中断请求线。
中断路由：I/O APIC根据配置将中断请求路由到本地APIC。
中断向量：本地APIC将中断请求转换为中断向量，并发送到CPU的中断引脚（LINT0或LINT1）。
中断应答：CPU响应中断请求，并从本地APIC读取中断向量，跳转到相应的中断处理程序。
